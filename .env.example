# Study HQ configuration
# Required for AI features (practice generation, flashcards, marking, exams, study plans).
# Get a key from https://console.anthropic.com  -> keep it server-side only.
ANTHROPIC_API_KEY=

# Optional overrides
ANTHROPIC_MODEL=claude-sonnet-5
API_PORT=4100
DATA_DIR=./data
